diff -up kopete-18.12.0/protocols/jabber/libjingle/talk/session/phone/v4llookup.cc.omv~ kopete-18.12.0/protocols/jabber/libjingle/talk/session/phone/v4llookup.cc
--- kopete-18.12.0/protocols/jabber/libjingle/talk/session/phone/v4llookup.cc.omv~	2018-12-14 20:54:52.556579678 +0100
+++ kopete-18.12.0/protocols/jabber/libjingle/talk/session/phone/v4llookup.cc	2018-12-14 20:55:17.929912292 +0100
@@ -15,6 +15,7 @@
 #include <sys/ioctl.h>
 #include <sys/types.h>
 #include <sys/stat.h>
+#include <sys/sysmacros.h>
 #include <unistd.h>
 
 #include <cstring>
@@ -35,8 +36,8 @@ bool V4LLookup::CheckIsV4L2Device(const
   bool is_v4l2 = false;
 
   // check major/minur device numbers are in range for video device
-  if (major(s.st_rdev) == 81) {
-    dev_t num = minor(s.st_rdev);
+  if (gnu_dev_major(s.st_rdev) == 81) {
+    dev_t num = gnu_dev_minor(s.st_rdev);
     if (num <= 63 && num >= 0) {
       video_fd = ::open(device_path.c_str(), O_RDONLY | O_NONBLOCK);
       if ((video_fd >= 0) || (errno == EBUSY)) {
